# ==================================================
# PEGASOI FRONTEND ENVIRONMENT VARIABLES
# ==================================================

# ----- Backend API Configuration -----
# URL of the deployed Pegasoi backend (Railway)
NEXT_PUBLIC_API_URL=https://pegasoibackend-production.up.railway.app

# GraphQL endpoint (if backend supports it)
NEXT_PUBLIC_GRAPHQL_URL=https://pegasoibackend-production.up.railway.app/graphql

# ----- Application Configuration -----
NEXT_PUBLIC_APP_NAME=Pegasoi Research Platform
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_APP_ENV=development

# ----- Authentication (Clerk) -----
# Get these from https://clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key

# Clerk Webhook Secret (for handling auth events)
CLERK_WEBHOOK_SECRET=whsec_your_webhook_secret

# ----- Web3 Configuration (Future Use) -----
# Wallet Connect Project ID (get from https://cloud.walletconnect.com/)
# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id

# Default blockchain network
# NEXT_PUBLIC_DEFAULT_CHAIN_ID=1

# Contract addresses (when deployed)
# NEXT_PUBLIC_RESEARCH_CONTRACT_ADDRESS=0x...
# NEXT_PUBLIC_TOKEN_CONTRACT_ADDRESS=0x...

# ----- Development & Debugging -----
# Enable debug logging
NEXT_PUBLIC_DEBUG=false

# Enable API request logging
NEXT_PUBLIC_LOG_API_REQUESTS=false

# ----- Analytics (Optional) -----
# Google Analytics ID
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# PostHog API Key
# NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxx
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# ----- External Services -----
# Rate limiting for API calls (requests per minute)
NEXT_PUBLIC_API_RATE_LIMIT=60

# Timeout for API requests (milliseconds)
NEXT_PUBLIC_API_TIMEOUT=30000

# ----- Feature Flags -----
# Enable/disable features during development
NEXT_PUBLIC_ENABLE_WEB3=false
NEXT_PUBLIC_ENABLE_EXPERIMENTAL_FEATURES=false
NEXT_PUBLIC_ENABLE_OFFLINE_MODE=false

# ----- Security -----
# Content Security Policy (CSP) settings
# NEXT_PUBLIC_CSP_REPORT_URI=https://your-csp-report-endpoint.com

# CORS origins for API requests (comma-separated)
NEXT_PUBLIC_ALLOWED_ORIGINS=http://localhost:3000,https://your-production-domain.com

# ==================================================
# INSTRUCTIONS:
# 1. Copy this file to .env.local
# 2. Update the values above with your actual configuration
# 3. Never commit .env.local to version control
# 4. For production deployment, set these in your hosting platform
# ==================================================
